import 'package:flutter/material.dart';

class UpsuPage extends StatelessWidget {
  const UpsuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('UPSU.net')),
      body: const Center(child: Text('UPSU.net page placeholder')),
    );
  }
}